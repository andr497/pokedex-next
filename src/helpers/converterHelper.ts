export function convertHectogramToKilogram(hg: number) {
    //1 hectogram = 0.1 kilograms
    return hg / 10;
}

export function convertDecimeterToMeter(mt: number) {
    //1 Decimeter = 0.1 meters
    return mt / 10;
}